import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Optionally add your own styles
import App from './App'; // Assuming App.js exists in your src folder

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
